namespace Assignment003
{
    internal class Program
    {

        #region Part1
        /*
         Q1:B (To define a blueprint for a class)
         
         Q2: A (Private)
         
         Q3: B (NO)

         Q4: B (Yes, interfaces can inherit from multiple interfaces)

         Q5: no answer we should use : t implement interface 

         Q6: A (Yes)

         Q7: B (No, all members are implicitly public)

         Q8: B (To provide a clear separation between interface and class members)

         Q9: B (No, interfaces cannot have constructors)

         Q10: C (By separating interface names with commas)


         */
        #endregion

        static void Main(string[] args)
        {
            #region Q1
            //Circle circle = new Circle(5);

            //Rectangle rect = new Rectangle(10, 8);

            //circle.DisplayInfo();
            //rect.DisplayInfo();
            #endregion

            #region Q2
            //BasicAuthenticationService service=new BasicAuthenticationService();

            //string username = "admin";
            //string password = "password123";
            //string role = "Administrat";

            //bool isauthenticated=service.AuthenticationUser(username,password);
            //if (isauthenticated)
            //{
            //    Console.WriteLine($"User:{username} Authenticated successfully");
            //}
            //else
            //{
            //    Console.WriteLine($"User:{username} Not Authenticated ");
            //}

            //bool isauthorized = service.AuthorizationUser(username, role);
            //if (isauthorized)
            //{
            //    Console.WriteLine($"User:{username} Authorized successfully");
            //}
            //else
            //{
            //    Console.WriteLine($"User:{username} Not Authorized ");
            //}
            #endregion

            #region Q3
            //EmailNotificationService emailService = new EmailNotificationService();
            //SmsNotificationService smsService = new SmsNotificationService();
            //PushNotificationService pushService= new PushNotificationService();

            //string recipient = "Ahmed";
            //string message = "Hello " + recipient;

            //emailService.SendNotification(recipient, message);
            //smsService.SendNotification(recipient, message);
            //pushService.SendNotification(recipient, message);


            #endregion


        }
    }
}
